

import 'package:flutter/material.dart';

class ConstWidget{

  static  BorderRadius borderRadiusPannel = BorderRadius.only(
    topLeft: Radius.circular(24),
    topRight: Radius.circular(24),
  );
}
